{
  "_comment": "Local development overlay. Uses UAT ICEGATE endpoints. Populate each dev client's IcegateClients[].EncryptedCredentialData via 'dotnet user-secrets set' - do not edit real values into this file. The single dev client below uses an obviously-fake key/credential so `dotnet run` works out of the box against a mocked or unreachable ICEGATE UAT.",

  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information"
    }
  },

  "Icegate": {
    "Environment": "UAT"
  },

  "IcegateClients": [
    {
      "ClientId": "LOCAL-DEV",
      "ApiKey": "local-dev-internal-key-change-me",
      "EncryptedCredentialData": "",
      "IcegateApiKey": "",
      "DefaultSenderId": "",
      "DefaultIcegateId": "",
      "DefaultCustodianCode": "",
      "Enabled": true
    }
  ],

  "ConnectionStrings": {
    "IcegateDb": "Data Source=icegate_dev.db"
  }
}
